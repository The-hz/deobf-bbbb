# JVM / build tuning
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.parallel=true

# JavaFX plugin options
javafx.platform=linux

# Project metadata
project.name=jarremapper
project.version=1.0.0
